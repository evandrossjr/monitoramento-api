version: '3.9'
services:
  monitor-api:
    build: .
    container_name: monitor-api
    restart: always
    ports:
      - '5000:5000'
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs